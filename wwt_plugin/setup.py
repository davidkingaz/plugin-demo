from setuptools import find_packages, setup

setup(
    name='wwt_plugin',
    version='0.1',
    description='WWT Plugin',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)